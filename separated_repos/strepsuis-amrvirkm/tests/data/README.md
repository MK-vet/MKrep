# Test Data Fixtures

This directory contains a synced copy of the example datasets used by integration and data validation tests. Files are copied from the shared `../../data/` directory to keep CI runs self-contained.
